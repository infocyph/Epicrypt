Passwords
=========

Password APIs are documented in :doc:`password`.

Key components:

- ``PasswordHasher`` for hash/verify/rehash lifecycle.
- ``PasswordPolicyValidator`` for policy enforcement.
- ``PasswordStrength`` for lightweight strength scoring.
- ``CompromisedPasswordCheckerInterface`` for pluggable breach checks.

