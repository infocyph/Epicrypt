Certificates
============

Certificate APIs are documented in :doc:`certificate`.

Includes:

- Key pair generation.
- CSR and certificate building.
- SAN support via ``CertificateOptions``.
- CA signing.
- Utility helpers (fingerprint, expiry, key match, chain verify, PEM normalize).

